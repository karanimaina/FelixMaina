import { CapitalizeSentencePipePipe } from './capitalize-sentence-pipe.pipe';

describe('CapitalizeSentencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeSentencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
